version: '3.8'

services:
  app:
    build: ./backend
    container_name: ecommerce_app
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - DB_HOST=mysql-db
      - DB_PORT=3306
      - DB_NAME=ecommerce_db
      - DB_USER=root
      - DB_PASSWORD=123456
    depends_on:
      mysql-db:
        condition: service_healthy
    networks:
      - ecommerce_network
    volumes:
      - app_uploads:/app/uploads

  mysql-db:
    image: mysql:8.0
    container_name: ecommerce_mysql
    ports:
      - "3307:3306"
    environment:
      - MYSQL_DATABASE=ecommerce_db
      - MYSQL_ROOT_PASSWORD=123456
    volumes:
      - db_data:/var/lib/mysql
      - ./docker/mysql/database.sql:/docker-entrypoint-initdb.d/1-schema.sql
    networks:
      - ecommerce_network
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p123456" ]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    build:
      context: ./frontend
    container_name: ecommerce_frontend
    ports:
      - "5173:5173"
    depends_on:
      - app
    networks:
      - ecommerce_network
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0

networks:
  ecommerce_network:
    driver: bridge

volumes:
  db_data:
  app_uploads:
